{
  "//": "测试专用 tsconfig。tsx 会把 tsconfig 的 paths 当运行时解析表；tsconfig.json 里为 tsc 准备的 upstream stub 别名（包括 pi-hashline-edit-pro 的子路径）会让真实依赖被解析成 .d.ts，测试随即报「does not provide an export named ...」。所以跑测试时只保留包级别的 stub 别名，去掉子路径那条。改 tsconfig.json 的 paths 时要同步这里。",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "baseUrl": "..",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "types": ["node"],
    "paths": {
      "@zhcsyncer/pi-tool-display-intent": ["./src/upstream.d.ts"],
      "pi-hashline-edit-pro": ["./src/upstream.d.ts"]
    }
  }
}
